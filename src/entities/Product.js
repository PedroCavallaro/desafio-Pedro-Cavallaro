class Product {
    constructor(cod, desc, value, parent) {
        (this.cod = cod),
            (this.desc = desc),
            (this.value = value),
            (this.parent = parent);
    }
}
export { Product };
